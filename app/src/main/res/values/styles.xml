<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/darkMainPrimaryColor</item>
        <item name="colorPrimaryDark">@color/darkMainPrimaryColor</item>
        <item name="colorAccent">@color/mainPrimaryColor</item>
        <item name="android:textColorSecondary">@color/white</item>
    </style>

</resources>
